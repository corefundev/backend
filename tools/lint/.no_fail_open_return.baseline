# NFOR — no-fail-open-return baseline
#
# Each line:  <path>:<lineno><TAB or two-spaces><reason>
#
# Adding to this list REQUIRES a written reason. The linter rejects any
# entry without one. Reasons should reference a triage ticket or audit
# finding so the allowlist stays auditable.
#
# Removing an entry is the *target state* for that line — the codebase
# becomes fail-closed when this file is empty.
#
# Initial baseline captured 2026-05-21 at R9-B kickoff. Established 51
# pre-existing sites; R9-B item #14 closes the high-risk subset
# (task_queue.py + train.py:301), the rest are triaged in R10.

# ── R9-B item #2 — CLOSED 2026-06-01 (R11) ─────────────────────────────
# train.py MLflow log swallow removed: extracted to _save_and_log_to_mlflow
# which RETURNS explicit (run_id, error) state; the error is persisted to
# sku_training_runs.mlflow_logging_error (migration 013) and surfaced in the
# training-runs API. No longer log-only → no baseline entry needed.

# ── R9-B item #14 — task_queue.py exception narrowing ─────────────────────
# CLOSED by #185 (2026-06-28): the degraded sync-training fallback (4 broad
# excepts doing partial run-status bookkeeping) was DELETED — the fallback now
# routes through `_training_job` (gated by ALLOW_SYNC_TRAINING_FALLBACK), whose
# error handling lives in named helpers. No task_queue.py NFOR entries remain.

# ── pre-R9 baseline — to be triaged in R10 maintenance ────────────────────
# Each of these should either get narrowed to a specific exception type,
# convert the swallow into a persisted error, or document the fail-open
# branch as deliberate (and then move out of "broad except" to a typed one).
src/pipeline/task_queue.py:209  NC-1 #241 — _run_pipeline_or_fail inbox-emit guard: best-effort notification row on failure; a notifications hiccup must not mask the original training error being re-raised (same class as the email/telegram sender guards)
src/pipeline/task_queue.py:216  pre-R9 baseline; triage in R10 — _run_pipeline_or_fail outer try (was :204, +12 NC-1 inbox emit)
src/pipeline/task_queue.py:230  pre-R9 baseline; triage in R10 — _run_pipeline_or_fail finalizer (was :218, +12 NC-1 inbox emit)
src/pipeline/train.py:315  pre-R9 baseline; triage in R10 — registry lookup in train pipeline (was :311, +4 #268)
src/pipeline/train.py:353  pre-R9 baseline; triage in R10 — plan-spec lookup in train pipeline (was :349, +4 #268)
src/pipeline/train.py:250  QW2-4 #227 gate infra fail-open (was :246, +4 #268 champion-marker comment)
src/pipeline/inference_utils.py:188  R12-#91 — holiday recompute is best-effort enhancement; on any failure it falls back to the prior carry-forward behaviour and the forecast still completes (core result unaffected) (was :195, -7 R13-7 dead-code removal)
src/pipeline/inference_utils.py:341  pre-R9 baseline (was :331, +10 L-A4 #186 doc-comment); triage in R10 — recursive forecast fallback
src/auth/otp.py:72  pre-R9 baseline; triage in R10 — OTP file race fallback
src/auth/vault_agent.py:264  pre-R9 baseline; triage in R10 — Lockbox bootstrap path A (+1 line after R10 Phase 0-B compose-DSN insertion)
src/auth/vault_agent.py:301  pre-R9 baseline; triage in R10 — Lockbox bootstrap path B (+1 line after R10 Phase 0-B)
src/auth/vault_agent.py:313  pre-R9 baseline; triage in R10 — Lockbox bootstrap path C (+1 line after R10 Phase 0-B)
src/auth/api_keys.py:151  R9-T4-006 — API key hash try; triage in R10 (audit-critical)
src/features/external_regressors_ru.py:141  pre-R9 baseline; triage in R10 — CBR FX feed best-effort
src/features/external_regressors_ru.py:157  pre-R9 baseline; triage in R10 — CBR FX feed best-effort
src/features/engineering.py:107  pre-R9 baseline; triage in R10 — feature engineering optional step (was :107, +18 #229 market block) (revert #229 block → module)
src/features/engineering.py:115  pre-R9 baseline; triage in R10 — feature engineering optional step (was :115, +18 #229 market block) (revert #229 block → module)
src/features/engineering.py:123  pre-R9 baseline; triage in R10 — feature engineering optional step (was :123, +18 #229 market block) (revert #229 block → module)
src/storage/upload_pipeline.py:178  pre-R9 baseline; triage in R10 — scan-stage error capture [+3 R13-LOW .xlsm comment]
src/storage/upload_pipeline.py:207  pre-R9 baseline; triage in R10 — scan-stage final [+3 R13-LOW .xlsm comment]
src/storage/upload_pipeline.py:294  pre-R9 baseline; triage in R10 — process-stage error capture [+3 R13-LOW .xlsm comment]
src/storage/upload_pipeline.py:307  pre-R9 baseline; triage in R10 — process-stage final [+3 R13-LOW .xlsm comment]
src/storage/upload_pipeline.py:313  R9-T7-010 — uploads error_message never set; triage in R10 [+3 R13-LOW .xlsm comment]
src/storage/upload_pipeline.py:374  pre-R9 baseline; triage in R10 — cancel_upload best-effort [+3 R13-LOW .xlsm comment]
src/storage/sandbox.py:163  pre-R9 baseline (was :240); R11-#66 moved the site into spawn_and_wait — container remove is best-effort cleanup AFTER logs are read, failure logged
src/storage/sandbox.py:326  pre-R9 baseline (was :228); R11-#66 refactor shifted — post-run persist error folds into stderr + ok=False, surfaced not swallowed
src/audit/log.py:257  R9-T4-004 — audit replica fallback; close as part of T6-006 audit HMAC fail-closed (+33 lines after R10 phantom-metric Counter insertion 2026-05-27)
src/audit/log.py:446  pre-R9 baseline; triage in R10 — list_for_client read fallback (+61 lines after R10 phantom-metric Counter)
src/audit/log.py:486  pre-R9 baseline; triage in R10 — recent_failed_logins read fallback (+61 lines after R10 phantom-metric Counter)
src/audit/log.py:566  pre-R9 baseline; triage in R10 — verify_chain read fallback (+61 lines R10 phantom-metric; +10 R13-3 no-gap, 2026-06-23)
src/api/routers/auth.py:468  pre-R9 baseline; triage in R10 — signup OAuth race retry (was :362, +63 after ARCH-2 #206 shared-guard helpers added above) (was :425, +38 ADM-7 H3 tripwire helper) (+5 ADM-10 token gate)
src/api/routers/config.py:284  pre-R9 baseline; triage in R10 — config-reset write fallback (was :201, +70 lines #95; +13 R13-2 horizon-clamp wiring, 2026-06-22)
src/api/routers/ops.py:151  pre-R9 baseline — /readyz dependency check is fail-CLOSED (sets ok=False → 503); broad except is correct here. (was :145, +5 CONTRACT-2 #208 probe-contract docstring) (+1 Optional import)
src/api/routers/plans.py:54  pre-R9 baseline; triage in R10 — sku-count lookup fallback
src/api/routers/notifications.py:157  pre-R9 baseline; triage in R10 — telegram_webhook intentional 200 (webhook ack semantics) (was :152, +4 #184 run_in_threadpool import+comment) (was :156, +1 R5-M6 hoist)
# (2 SKU-cap pre-check swallows CLOSED 2026-06-02 by R11-H3 — the manifest
#  check is now fail-closed: it raises HTTPException instead of swallowing,
#  so it's no longer log-only. The 3 remaining sites below shifted +24 lines.)
src/api/routers/training.py:313  pre-R9 baseline; triage in R10 — trigger_training mid-flow (+26 lines after R11-H3 fail-closed cap + docstring, 2026-06-02) (was :294, NC-5 emits)
src/api/routers/training.py:342  pre-R9 baseline; triage in R10 — trigger_training enqueue (was :334, -11 lines after #73 dropped the counter-refund, 2026-06-18) (was :323, NC-5 emits)
src/api/routers/training.py:355  pre-R9 baseline; triage in R10 — trigger_training fallback (was :347, -11 lines after #73, 2026-06-18) (was :336, NC-5 emits)
src/data/anomaly_detection.py:78  pre-R9 baseline; triage in R10 — anomaly detection optional
src/pipeline/inference_utils.py:414  R11-#59 (was :404, +10 L-A4 #186 doc-comment) — direct_forecast: p10/p90 are an OPTIONAL band; a predict_quantiles failure must not drop the point forecast (best-effort, core result unaffected)
src/pipeline/inference_utils.py:475  R11-#59 (was :460, +10 L-A4 #186 doc-comment) — serve_forecast: intentional fallback — if direct prediction raises, drop to recursive_forecast (carries the SeasonalNaive fallback). The fall-through IS the designed safety path (was :470, +5 #229 choke merge)
src/pipeline/inference_utils.py:487  QW2-1 #224 — serve_forecast chained branch: if the direct HEAD-SEGMENT raises, head_rows=[] and the function falls through to the full-recursive serve — same designed-safety-path class as :470 (the fall-through IS the fallback) (was :482, +5 #229 choke merge)

src/pipeline/reconcile_runs.py:100  #265 — reconcile per-sender notify guard: the heal (status=failed) is ALREADY persisted above; a sender failure must not abort healing the remaining runs
src/pipeline/reconcile_runs.py:103  #265 — reconcile notifications outer guard: same rationale; healing is the contract, notification is best-effort (NC-1 discipline)
src/api/routers/clients.py:455  ADM-3 #256 — overview logins-enrichment guard: recent logins are best-effort; their DB failure must not blank the client PROFILE (the card's core); logged loudly
src/api/routers/clients.py:463  ADM-3 #256 — overview runs-enrichment guard: same rationale as :455
src/notifications/staleness_nudge.py:108  NC-3 #243 — email-push guard: push is best-effort (NC-1 sender discipline); a sender failure must not abort the remaining clients' nudges; logged loudly
src/notifications/staleness_nudge.py:116  NC-3 #243 — telegram-push guard: same rationale as :108
src/api/routers/ops.py:279  ADM-13 #281 — system-page postgres ping guard: NOT swallowed — the exception type is captured INTO the response ("error: X") and rendered to the operator; the health page must degrade per-component, never 500
src/api/routers/ops.py:304  ADM-13 #281 — system-page pushgateway guard: same rationale as :278 (redis guard at :286 uses get_redis_or_none which never raises past it)
src/api/routers/ops.py:273  ADM-13 #281 — system-page redis ping guard: exception type captured INTO the response, rendered to the operator (same class as :278)
