# ══════════════════════════════════════════════════════
# SKU Forecasting Platform v8.0 — зависимости
#
# Принцип: фиксировать только ПРЯМЫЕ зависимости.
# Transitive deps (opentelemetry, protobuf и т.д.) —
# указывать диапазон или оставлять без версии.
# Это позволяет pip разрешить конфликты автоматически.
#
# Проверенные совместимые комбинации:
#   mlflow 3.13.0 + pyarrow 23.0.1 (mlflow 3.13 allows pyarrow<25,>=4;
#     mlflow 3.13 also caps numpy<3 / scipy<2 / sklearn<2 / pandas<3,
#     so the existing pins + the scipy<2 evidently wedge all hold)
#   evidently 0.5.0 + opentelemetry-api>=1.25.0
# ══════════════════════════════════════════════════════

# ── Ядро ML ──────────────────────────────────────────
lightgbm==4.6.0            # CVE-2024-43598 (RCE) fixed in 4.6
# (catboost removed 2026-06-03, R11-M15 — imported nowhere in src/ or
#  tests/; a ~100MB native wheel pulled into every image for nothing.)
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.4.2
# scipy: NOT a first-party import (grep src/ = 0). Pinned as a RESOLVER
# CONSTRAINT — the scipy<2 wedge keeps evidently 0.5.0 + sklearn/shap
# resolvable (R6-1, deferred by user 2026-05-18; re-trigger only on
# evidently 0.6+ / a scipy CVE). Do NOT remove — dropping it lets the
# resolver float scipy to 2.x and break evidently.
scipy==1.13.0
# Audit R4-19 (2026-05-17): pyarrow 15.0.2 → 17.0.0 closed PYSEC-2024-161
# (CVE-2024-52338). R11-#81 (2026-06-08): 17.0.0 → 23.0.1 closes
# CVE-2026-25087 (apache-arrow C++ DoS, HIGH per trivy; first fixed in
# 23.0.1). Required bumping mlflow 3.11.1 → 3.13.0 (3.11 capped pyarrow≤19;
# 3.13 allows pyarrow<25). Bumped sandbox's requirements-sandbox.txt in
# LOCKSTEP so the parquet IPC format stays consistent across the
# sandbox-write → main-pipeline-read boundary (R3-3 lesson: pyarrow version
# skew silently breaks deserialization).
pyarrow==23.0.1           # 23.0.1: closes CVE-2026-25087; within mlflow 3.13's pyarrow<25

# ── API ───────────────────────────────────────────────
# Bumped past 0.118 because Starlette 0.40 caught CVE-2025-62727
# (DoS via Range header merging) — 0.49.1 is the patched version,
# and FastAPI 0.121 is the first to widen the cap to allow it.
fastapi==0.121.0
starlette==0.49.1          # CVE-2024-47874 + CVE-2025-62727
# Explicit `python-multipart` pin (2026-05-15) — fastapi's UploadFile +
# Form handling looks for this package. Without an explicit pin, pip
# may install the unrelated `multipart` PyPI package as a transitive,
# which fastapi detects and refuses with a clear error.
# Bumped 0.0.18 → 0.0.27 (2026-05-15 follow-up) — closes
#   CVE-2026-24486: arbitrary file write via path traversal (fixed in 0.0.22)
#   CVE-2026-42561: DoS via unbounded multipart part headers (fixed in 0.0.27)
# Both flagged by trivy as HIGH; 0.0.27 is the lowest version that
# clears both.
python-multipart==0.0.27
uvicorn[standard]==0.29.0
pydantic==2.7.1
# R5-M4 — central Settings class (src/settings.py) uses BaseSettings
# from the split-out pydantic-settings package (pydantic 2.x moved it
# out of the core library). Pinned to a version compatible with
# pydantic 2.7.x; bumping pydantic core requires re-verifying this pin.
pydantic-settings==2.3.4
httpx==0.27.0

# ── Аутентификация и безопасность ────────────────────
PyJWT==2.13.0              # R12-#85: CVE-2026-48526 + 48522-25 fixed in 2.13; CVE-2026-32597 fixed in 2.12
hvac==2.3.0
# R12-#89 (M1): imported directly (lockbox_agent PS256 signing, Google
# OIDC RS256 verify) but previously rode in as mlflow's transitive —
# a security-critical primitive must be pinned explicitly. Matches the
# version mlflow 3.13 resolves (<49,>=43); bump deliberately, not by
# resolver accident.
cryptography==48.0.1

# ── Хранилище ─────────────────────────────────────────
boto3==1.34.100

# ── Валидация данных ──────────────────────────────────
great-expectations==0.18.15

# ── ML трекинг ────────────────────────────────────────
# Bumped to 3.11 to clear the bundle of CVEs the 2.14 line carried
# (CVE-2025-15036 path traversal, CVE-2025-15379 / CVE-2025-14287
# command execution, CVE-2025-11200 auth bypass, etc).
# Client API (start_run / log_metric / log_artifact) is unchanged
# from 2.x; the major-version bump is mostly server-side.
# R11-#81 (2026-06-08): 3.11.1 → 3.13.0 — 3.11 capped pyarrow≤19, blocking
# the pyarrow 23.0.1 bump needed for CVE-2026-25087. 3.13 widens to
# pyarrow<25 while keeping numpy<3 / scipy<2 / sklearn<2 / pandas<3, so
# every existing pin (incl. the scipy<2 evidently wedge) still resolves.
mlflow==3.13.0

# ── Очередь задач ─────────────────────────────────────
redis==5.0.4
rq==1.16.2

# ── Upload pipeline (sandbox / antivirus) ─────────────
# docker SDK: process-worker spawns secure_parser containers.
# clamd protocol is implemented in-project (scanner.py) — no extra dep.
docker==7.1.0

# ── Per-client API keys (Phase 6) ─────────────────────
# bcrypt for hashing client api_keys at rest. Constant-time verify
# is the whole point — must hash + compare, never plain-text store.
bcrypt==4.2.0

# ── Мониторинг ────────────────────────────────────────
prometheus-client==0.20.0
evidently==0.5.0           # 0.5.x: стабильный API, совместим с otel>=1.25

# ── База данных ───────────────────────────────────────
# psycopg2-binary 2.9.9 → 2.9.11 (audit R2-29, 2026-05-15) —
# cumulative bug fixes; no semver-breaking changes documented.
psycopg2-binary==2.9.11
asyncpg==0.29.0

# ── Конфигурация ─────────────────────────────────────
pyyaml==6.0.1

# ── ML улучшения ─────────────────────────────────────
optuna==3.6.1
# Audit R3-17 — bumped 0.46.0 → 0.49.1 (2026-05-16). Conservative
# step inside the 0.4x line: last patch release before the 0.50
# numpy-2 / API-refactor cycle. TreeExplainer + shap_values surface
# unchanged since 0.30; TestSKUExplainer in test_improvements.py
# (test_global_importance_columns / test_explain_row_returns_list /
# test_explain_row_direction_correct) covers the structural contract.
shap==0.49.1
holidays==0.46
# (mapie removed 2026-06-03, R11-M15 — imported nowhere. dvc/dvc-s3
#  removed 2026-06-05, R11-#71 — their only consumer src/data/versioning.py
#  was deleted as dead code and there is no .dvc/ config / S3 remote.)

# ── Observability ─────────────────────────────────────
# Audit R3-10 — major-cap pins. Earlier we used bare `>=` because the
# combinatorial resolver between mlflow + evidently + otel kept flapping.
# `>=1.25.0,<2.0.0` keeps the auto-resolver happy (any 1.25.x..1.x.x is
# fine) but blocks an unsupervised jump to a hypothetical otel 2.x that
# would almost certainly break the instrumentation contract.
opentelemetry-api>=1.25.0,<2.0.0
opentelemetry-sdk>=1.25.0,<2.0.0
opentelemetry-instrumentation-fastapi>=0.45b0,<1.0.0
# OTLP/gRPC exporter — without this, src/monitoring/tracing.py falls
# back to console traces even when OTEL_EXPORTER_OTLP_ENDPOINT is set.
opentelemetry-exporter-otlp-proto-grpc>=1.25.0,<2.0.0
python-json-logger==2.0.7

# ── Тестирование ──────────────────────────────────────
pytest==8.2.0
pytest-asyncio==0.23.6

# ── Линтинг и типизация ───────────────────────────────
flake8==7.1.0
mypy==1.10.0
types-PyYAML==6.0.12.20240311
types-redis==4.6.0.20241004

# ── Build-tool security pins ──────────────────────────
# These come baked into the Python image and aren't direct deps of
# our code; we pin newer versions only to clear Trivy CVEs found
# in the resulting layer (CVE-2026-24049 wheel priv-esc,
# CVE-2026-23949 jaraco.context path traversal). Both are pulled
# by pip itself — overriding via requirements.txt is the simplest
# way to bump them.
wheel==0.46.2
jaraco.context==6.1.0
